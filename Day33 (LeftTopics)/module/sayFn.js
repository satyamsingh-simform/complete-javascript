//default export can be imported with diff name that the diff bw name export and default export 
// and also same name can be used that understandable

const sayName=()=>{
    return 'Satya';
}

export default sayName;
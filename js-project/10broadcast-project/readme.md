<!-- broadcast api in js -->
The Broadcast channel API allow basic communication bw Browsing context(i.e.,window, tab or iframe) provide tab have same origin.
when to use 
    for user log in or log out then it should be login/logout from all tab of that site.
    same dark theme